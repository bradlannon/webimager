---
phase: 11-blur-sharpen-safari-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/editor.ts
  - src/utils/canvas.ts
  - src/hooks/useRenderPipeline.ts
  - src/utils/download.ts
  - src/store/editorStore.ts
  - src/main.tsx
  - src/__tests__/canvas.test.ts
  - src/__tests__/renderPipeline.test.ts
  - src/__tests__/download.test.ts
  - package.json
autonomous: true
requirements:
  - COMPAT-01
must_haves:
  truths:
    - "renderToCanvas accepts an options object instead of 7 positional parameters"
    - "All existing call sites (useRenderPipeline, downloadImage, applyResize) use the new options API"
    - "All existing tests pass with the new signature"
    - "context-filter-polyfill is installed and imported at app startup"
    - "ctx.filter assignments work on Safari (polyfill patches drawing methods)"
  artifacts:
    - path: "src/utils/canvas.ts"
      provides: "renderToCanvas with RenderOptions parameter, buildFilterString unchanged"
      contains: "RenderOptions"
    - path: "src/main.tsx"
      provides: "Polyfill import at app entry"
      contains: "context-filter-polyfill"
  key_links:
    - from: "src/hooks/useRenderPipeline.ts"
      to: "src/utils/canvas.ts"
      via: "renderToCanvas(ctx, sourceImage, options)"
      pattern: "renderToCanvas\\(ctx.*\\{.*transforms"
    - from: "src/utils/download.ts"
      to: "src/utils/canvas.ts"
      via: "renderToCanvas(ctx, source, options)"
      pattern: "renderToCanvas\\(ctx.*\\{.*transforms"
    - from: "src/main.tsx"
      to: "context-filter-polyfill"
      via: "side-effect import"
      pattern: "import 'context-filter-polyfill'"
---

<objective>
Refactor renderToCanvas from 7 positional parameters to a single RenderOptions object, and install the Safari ctx.filter polyfill.

Purpose: Prevent signature churn across Phases 11-14 (each adds new options), and fix the pre-existing Safari bug where ALL adjustments (brightness, contrast, saturation) silently fail because ctx.filter is disabled by default through Safari 26.4.

Output: Clean options-based renderToCanvas API, all call sites updated, polyfill imported, all tests green.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-blur-sharpen-safari-compatibility/11-RESEARCH.md

<interfaces>
<!-- Current signatures that will be refactored -->

From src/types/editor.ts:
```typescript
export interface Transforms {
  rotation: 0 | 90 | 180 | 270;
  freeRotation: number;
  flipH: boolean;
  flipV: boolean;
}

export interface Adjustments {
  brightness: number;
  contrast: number;
  saturation: number;
  greyscale: boolean;
}

export interface CropRegion {
  x: number;
  y: number;
  width: number;
  height: number;
}
```

From src/utils/canvas.ts (CURRENT signature to refactor):
```typescript
export function renderToCanvas(
  ctx: CanvasRenderingContext2D,
  source: ImageBitmap,
  transforms: Transforms,
  adjustments?: Adjustments,
  crop?: CropRegion,
  backgroundMask?: ImageData | null,
  replacementColor?: string | null
): void
```

Call sites to update:
1. src/hooks/useRenderPipeline.ts line 34: renderToCanvas(ctx, sourceImage, transforms, adjustments, activeCrop, activeMask, activeReplacementColor)
2. src/utils/download.ts line 23: renderToCanvas(ctx, source, transforms, adjustments, crop, backgroundMask, replacementColor)
3. src/store/editorStore.ts line 199: renderToCanvas(ctx, state.sourceImage, state.transforms, state.adjustments, state.cropRegion ?? undefined, ...)
4. src/__tests__/canvas.test.ts lines 101 and 141: renderToCanvas(trackedCtx, source, transforms, defaultAdjustments, undefined, mockMask, ...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install polyfill and refactor renderToCanvas to options object</name>
  <files>package.json, src/main.tsx, src/types/editor.ts, src/utils/canvas.ts</files>
  <action>
1. Install context-filter-polyfill: `npm install context-filter-polyfill`

2. Add side-effect import at top of src/main.tsx (before all other imports):
   `import 'context-filter-polyfill';`

3. Add RenderOptions interface to src/types/editor.ts (after CropRegion):
```typescript
export interface RenderOptions {
  transforms: Transforms;
  adjustments?: Adjustments;
  crop?: CropRegion;
  backgroundMask?: ImageData | null;
  replacementColor?: string | null;
}
```

4. Refactor renderToCanvas in src/utils/canvas.ts:
   - Change signature from 7 positional params to: `renderToCanvas(ctx: CanvasRenderingContext2D, source: ImageBitmap, options: RenderOptions): void`
   - Import RenderOptions from types/editor.ts
   - Destructure options at function start: `const { transforms, adjustments, crop, backgroundMask, replacementColor } = options;`
   - All internal logic remains identical -- this is a pure signature refactor
   - Update the import of types to include RenderOptions

Do NOT change buildFilterString -- it stays as-is for now.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>RenderOptions interface exists, renderToCanvas uses options object, polyfill installed and imported. TypeScript compilation will fail until call sites are updated (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Update all call sites and tests to use options API</name>
  <files>src/hooks/useRenderPipeline.ts, src/utils/download.ts, src/store/editorStore.ts, src/__tests__/canvas.test.ts, src/__tests__/renderPipeline.test.ts, src/__tests__/download.test.ts</files>
  <action>
1. Update src/hooks/useRenderPipeline.ts:
   - Change line 34 from: `renderToCanvas(ctx, sourceImage, transforms, adjustments, activeCrop, activeMask, activeReplacementColor)`
   - To: `renderToCanvas(ctx, sourceImage, { transforms, adjustments, crop: activeCrop, backgroundMask: activeMask, replacementColor: activeReplacementColor })`

2. Update src/utils/download.ts:
   - Change the renderToCanvas call from positional args to options object:
     `renderToCanvas(ctx, source, { transforms, adjustments, crop, backgroundMask, replacementColor })`

3. Update src/store/editorStore.ts (applyResize method, ~line 199):
   - Change from: `renderToCanvas(ctx, state.sourceImage, state.transforms, state.adjustments, state.cropRegion ?? undefined, ...)`
   - To: `renderToCanvas(ctx, state.sourceImage, { transforms: state.transforms, adjustments: state.adjustments, crop: state.cropRegion ?? undefined, backgroundMask: state.backgroundRemoved ? state.backgroundMask : undefined, replacementColor: state.backgroundRemoved ? state.replacementColor : undefined })`

4. Update src/__tests__/canvas.test.ts:
   - Line 101: Change `renderToCanvas(trackedCtx, source, transforms, defaultAdjustments, undefined, mockMask, '#ff0000')` to `renderToCanvas(trackedCtx, source, { transforms, adjustments: defaultAdjustments, backgroundMask: mockMask, replacementColor: '#ff0000' })`
   - Line 141: Change `renderToCanvas(trackedCtx, source, transforms, defaultAdjustments, undefined, mockMask, null)` to `renderToCanvas(trackedCtx, source, { transforms, adjustments: defaultAdjustments, backgroundMask: mockMask, replacementColor: null })`

5. Check src/__tests__/renderPipeline.test.ts and src/__tests__/download.test.ts for any direct renderToCanvas calls and update those too.

6. Verify TypeScript compiles and all tests pass.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx tsc --noEmit && npx vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>All call sites use RenderOptions object. TypeScript compiles cleanly. All existing tests pass. No behavioral changes -- pure refactor.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- zero TypeScript errors
2. `npx vitest run` -- all existing tests pass
3. `grep -rn "renderToCanvas(" src/ --include="*.ts" --include="*.tsx" | grep -v "export function\|import\|test\|\.test\."` -- all call sites use options object (no positional args remain)
4. `grep "context-filter-polyfill" src/main.tsx` -- polyfill import present
5. `grep "context-filter-polyfill" package.json` -- dependency installed
</verification>

<success_criteria>
- renderToCanvas signature is `(ctx, source, options: RenderOptions)`
- RenderOptions interface exported from types/editor.ts
- All 3 production call sites updated (useRenderPipeline, download, editorStore)
- All test call sites updated
- context-filter-polyfill installed and imported in main.tsx
- All existing tests pass unchanged (behavior preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/11-blur-sharpen-safari-compatibility/11-01-SUMMARY.md`
</output>
