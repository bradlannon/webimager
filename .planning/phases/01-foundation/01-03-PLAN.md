---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/components/TransformControls.tsx
  - src/components/DownloadPanel.tsx
  - src/components/PrivacyBadge.tsx
  - src/components/Sidebar.tsx
  - src/utils/download.ts
  - src/__tests__/download.test.ts
  - src/__tests__/components.test.ts
autonomous: false
requirements:
  - FILE-04
  - TRAN-02
  - TRAN-03
  - UX-02

must_haves:
  truths:
    - "User can rotate image 90 degrees left and right with instant preview"
    - "User can flip image horizontally and vertically with instant preview"
    - "User can reset all transforms back to original"
    - "User can download processed image as JPEG with quality selection"
    - "User can download processed image as PNG"
    - "Privacy indicator shows photo never leaves the browser"
    - "After rotating, canvas resizes to fit the rotated image (no clipping)"
  artifacts:
    - path: "src/components/TransformControls.tsx"
      provides: "Rotate left/right, flip H/V buttons, Reset all button"
      min_lines: 30
    - path: "src/components/DownloadPanel.tsx"
      provides: "Format picker (JPEG/PNG), quality slider, download button"
      min_lines: 40
    - path: "src/components/PrivacyBadge.tsx"
      provides: "Lock icon + privacy text"
      min_lines: 10
    - path: "src/utils/download.ts"
      provides: "Download utility using toBlob + createObjectURL"
      exports: ["downloadImage"]
  key_links:
    - from: "src/components/TransformControls.tsx"
      to: "src/store/editorStore.ts"
      via: "calls rotateLeft, rotateRight, flipHorizontal, flipVertical, resetAll"
      pattern: "useEditorStore.*rotate|flip|reset"
    - from: "src/components/DownloadPanel.tsx"
      to: "src/utils/download.ts"
      via: "calls downloadImage with current state"
      pattern: "downloadImage"
    - from: "src/utils/download.ts"
      to: "src/utils/canvas.ts"
      via: "calls renderToCanvas on offscreen canvas"
      pattern: "renderToCanvas"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/TransformControls.tsx"
      via: "renders inside collapsible Transform section"
      pattern: "TransformControls"
---

<objective>
Add transform controls (rotate/flip), download panel (format + quality), and privacy badge to complete the Phase 1 feature set. Verify the full end-to-end flow visually.

Purpose: This plan wires the store actions (built in Plan 01) to UI controls and adds the download pipeline, completing all Phase 1 requirements.
Output: Fully functional image editor with upload, rotate/flip, download, and privacy indicator.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 + 02 outputs -->

From src/store/editorStore.ts:
```typescript
// Actions available:
setImage(bitmap: ImageBitmap, file: File, wasDownscaled: boolean): void;
rotateLeft(): void;
rotateRight(): void;
flipHorizontal(): void;
flipVertical(): void;
resetAll(): void;
// State selectors:
sourceImage: ImageBitmap | null;
originalFile: File | null;
transforms: Transforms;
```

From src/utils/canvas.ts:
```typescript
export function renderToCanvas(ctx: CanvasRenderingContext2D, source: ImageBitmap, transforms: Transforms): void;
```

From src/types/editor.ts:
```typescript
export interface Transforms {
  rotation: 0 | 90 | 180 | 270;
  flipH: boolean;
  flipV: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TransformControls, DownloadPanel, PrivacyBadge, and download utility</name>
  <files>src/components/TransformControls.tsx, src/components/DownloadPanel.tsx, src/components/PrivacyBadge.tsx, src/components/Sidebar.tsx, src/utils/download.ts, src/__tests__/download.test.ts, src/__tests__/components.test.ts</files>
  <behavior>
    - downloadImage creates an offscreen canvas, renders transforms, exports as blob, triggers download
    - downloadImage uses toBlob (not toDataURL) per research anti-patterns
    - downloadImage passes quality param only for JPEG, undefined for PNG
    - downloadImage generates filename like "edited.jpg" or "edited.png"
    - TransformControls renders rotate-left, rotate-right, flip-H, flip-V, and Reset all buttons
    - PrivacyBadge renders text about photo never leaving the browser
  </behavior>
  <action>
1. Create `src/utils/download.ts`:
   - Export `downloadImage(source: ImageBitmap, transforms: Transforms, format: 'image/jpeg' | 'image/png', quality: number): void`
   - Create offscreen canvas via `document.createElement('canvas')`
   - Call `renderToCanvas(ctx, source, transforms)` to apply current transforms
   - Use `canvas.toBlob()` (NOT toDataURL -- per research anti-patterns)
   - For JPEG: pass quality param (0-1). For PNG: pass undefined for quality (PNG is always lossless)
   - Create download via `URL.createObjectURL(blob)` + anchor click + `URL.revokeObjectURL(url)`
   - Filename: use original filename stem if available from store, otherwise "edited", with correct extension (.jpg or .png)

2. Create `src/components/TransformControls.tsx`:
   - Icon buttons using lucide-react: RotateCcw (rotate left), RotateCw (rotate right), FlipHorizontal, FlipVertical
   - Each button calls the corresponding store action
   - "Reset all" button (RefreshCw icon or text button) calls `resetAll()`
   - Per user decision: all transforms apply instantly, no animations
   - Buttons styled with Tailwind: hover states, dark mode aware, grouped logically
   - Show current rotation state as subtle indicator (e.g., "90 degrees" text when rotated)

3. Create `src/components/DownloadPanel.tsx`:
   - Format picker: radio buttons or segmented control for JPEG / PNG
   - Quality slider: range input 1-100, only visible when JPEG is selected (per research pitfall 5: quality param ignored for PNG)
   - Download button with Download icon from lucide-react
   - Calls `downloadImage()` with current sourceImage, transforms, selected format, and quality/100
   - Disabled state when no image is loaded

4. Create `src/components/PrivacyBadge.tsx`:
   - Small lock icon (Lock or Shield from lucide-react) + text: "Your photo never leaves this browser"
   - Styled subtly -- not prominent, but visible. Dark mode aware.
   - Placed in sidebar footer or bottom area

5. Update `src/components/Sidebar.tsx`:
   - Replace placeholder text with actual components inside collapsible sections
   - Transform section: renders `<TransformControls />`
   - Download section: renders `<DownloadPanel />`
   - Footer area: renders `<PrivacyBadge />`

6. Write tests FIRST:
   - `src/__tests__/download.test.ts`: Test downloadImage creates canvas, calls renderToCanvas, calls toBlob with correct format and quality. Mock document.createElement, canvas methods, URL.createObjectURL/revokeObjectURL.
   - `src/__tests__/components.test.ts`: Test PrivacyBadge renders privacy text. Test TransformControls renders all expected buttons (rotate left, rotate right, flip H, flip V, reset).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Transform controls wired to store actions. Download produces correct JPEG/PNG files. Quality slider only shown for JPEG. Privacy badge visible in sidebar. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 flow</name>
  <files>none</files>
  <action>
Start the dev server with `npm run dev` and present the verification checklist to the user. This is a human verification checkpoint -- no code changes, only visual and functional confirmation of the complete Phase 1 flow.
  </action>
  <verify>User confirms all 15 verification steps pass by typing "approved"</verify>
  <done>User has approved the complete Phase 1 flow: upload, transforms, download, responsive layout, dark mode, and privacy badge all working correctly.</done>
  <what-built>Complete Phase 1: upload pipeline, canvas renderer, rotate/flip transforms, JPEG/PNG download with quality, responsive editor layout, privacy badge, and system dark/light mode.</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 -- should see full-page drop zone with "Drop your photo here or click to browse"
    2. Drag a JPEG onto the drop zone -- dashed border should highlight blue during drag
    3. Image should appear on canvas with checkerboard background behind it
    4. Try dragging a .txt file -- inline error should appear (no popup, no navigation)
    5. In sidebar Transform section: click rotate right 1x -- image rotates 90 degrees, canvas resizes to fit
    6. Click rotate right again -- 180 degrees
    7. Click flip horizontal -- image mirrors
    8. Click "Reset all" -- image returns to original orientation
    9. In sidebar Download section: select JPEG, adjust quality slider, click Download -- file downloads
    10. Select PNG -- quality slider should disappear. Click Download -- PNG downloads
    11. Verify privacy badge is visible: "Your photo never leaves this browser"
    12. Resize browser window narrow -- sidebar should move to bottom
    13. Toggle OS dark/light mode -- app theme should follow
    14. Click "New image" -- should return to drop zone
    15. Upload a photo taken on a phone (portrait orientation) -- should display upright, not sideways
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Upload -> rotate -> flip -> download produces correctly transformed image
- JPEG download with quality slider works (smaller file at lower quality)
- PNG download works (no quality param)
- Privacy badge visible
- Responsive layout works (sidebar moves to bottom on narrow screens)
- System dark/light mode works
- Reset returns to original state
- New image returns to drop zone
- All vitest tests pass
</verification>

<success_criteria>
- All 8 Phase 1 requirements (FILE-01 through FILE-04, TRAN-02, TRAN-03, UX-01, UX-02) are demonstrably working
- Full test suite passes
- User has visually verified the complete flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
