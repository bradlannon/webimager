---
phase: 02-adjustments
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/components/AdjustmentControls.tsx
  - src/components/Sidebar.tsx
autonomous: false
requirements:
  - ADJT-01
  - ADJT-02
  - ADJT-03
  - ADJT-04

must_haves:
  truths:
    - "User can drag brightness slider and see the image update in real-time"
    - "User can drag contrast slider and see the image update in real-time"
    - "User can drag saturation slider and see the image update in real-time"
    - "User can click greyscale button and see the image convert to black and white"
    - "All four adjustments compose correctly -- user can combine brightness + contrast + saturation + greyscale"
    - "Sliders show current percentage value"
    - "Double-clicking a slider label resets that slider to 100"
  artifacts:
    - path: "src/components/AdjustmentControls.tsx"
      provides: "Slider controls for brightness/contrast/saturation and greyscale toggle button"
      min_lines: 40
    - path: "src/components/Sidebar.tsx"
      provides: "Adjustments section wired into sidebar between Transform and Download"
      contains: "AdjustmentControls"
  key_links:
    - from: "src/components/AdjustmentControls.tsx"
      to: "src/store/editorStore.ts"
      via: "useEditorStore for adjustments state and setAdjustment/toggleGreyscale actions"
      pattern: "useEditorStore"
    - from: "src/components/Sidebar.tsx"
      to: "src/components/AdjustmentControls.tsx"
      via: "imports and renders inside CollapsibleSection"
      pattern: "AdjustmentControls"
---

<objective>
Create the AdjustmentControls UI component with brightness, contrast, saturation sliders and greyscale toggle button, then wire it into the Sidebar.

Purpose: Give users the visual controls to adjust their photos with real-time preview via the data layer built in Plan 01.
Output: Working adjustment panel in the sidebar with live preview.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-adjustments/02-RESEARCH.md
@.planning/phases/02-adjustments/02-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan depends on. -->

From src/types/editor.ts (added by Plan 01):
```typescript
export interface Adjustments {
  brightness: number;   // 0-200, default 100
  contrast: number;     // 0-200, default 100
  saturation: number;   // 0-200, default 100
  greyscale: boolean;   // false = off, true = full greyscale
}

export const defaultAdjustments: Adjustments;
```

From src/store/editorStore.ts (added by Plan 01):
```typescript
// Added to EditorStore interface:
adjustments: Adjustments;
setAdjustment: (key: keyof Omit<Adjustments, 'greyscale'>, value: number) => void;
toggleGreyscale: () => void;
```

From src/components/Sidebar.tsx (existing):
```typescript
// CollapsibleSection is a local component in Sidebar.tsx
// Currently renders: Transform, Download sections
// AdjustmentControls should be added between Transform and Download
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AdjustmentControls component and wire into Sidebar</name>
  <files>src/components/AdjustmentControls.tsx, src/components/Sidebar.tsx</files>
  <action>
    1. Create src/components/AdjustmentControls.tsx:
       - Import useEditorStore, Palette icon from lucide-react
       - Read adjustments, setAdjustment, toggleGreyscale from useEditorStore
       - Create a reusable slider row pattern (NOT a separate component -- just a local helper or repeated JSX):
         - Label on left with double-click handler to reset that value to 100 (title="Double-click to reset")
         - Current percentage value on right (tabular-nums for alignment)
         - Native `<input type="range">` min=0 max=200 step=1 below
         - Use proper `<label htmlFor>` + `id` pairing for accessibility
       - Render three sliders in order: Brightness, Contrast, Saturation
       - Below sliders, render Greyscale toggle button:
         - Full-width button with Palette icon
         - Active state: blue background, white text (bg-blue-500 text-white)
         - Inactive state: neutral background matching existing button patterns in TransformControls
         - onClick calls toggleGreyscale()
       - Style sliders with Tailwind: `w-full h-1.5 rounded-lg appearance-none cursor-pointer bg-neutral-200 dark:bg-neutral-600 accent-blue-500 dark:accent-blue-400`
       - Use `space-y-3` for vertical spacing between slider groups, `mt-3` before greyscale button

    2. Update src/components/Sidebar.tsx:
       - Import AdjustmentControls from './AdjustmentControls'
       - Add a new CollapsibleSection titled "Adjustments" between "Transform" and "Download" sections:
         ```
         <CollapsibleSection title="Adjustments">
           <AdjustmentControls />
         </CollapsibleSection>
         ```

    3. Verify the app builds and renders without errors:
       - `npx tsc --noEmit` passes
       - `npx vite build` succeeds
       - `npx vitest run` all tests still pass
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx tsc --noEmit && npx vitest run --reporter=verbose && npx vite build</automated>
  </verify>
  <done>AdjustmentControls component renders brightness/contrast/saturation sliders and greyscale button, wired into Sidebar between Transform and Download, app builds and all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify adjustment controls work end-to-end</name>
  <action>
    Human verifies the complete adjustment UI: brightness, contrast, saturation sliders with live preview, greyscale toggle button, composition with existing transforms, and download with adjustments applied.

    What was built: Brightness/contrast/saturation sliders (0-200 range, 100% neutral), greyscale toggle button, all composable with each other and with rotate/flip transforms. Download includes adjustments.
  </action>
  <verify>
    1. Run `cd /Users/brad/Apps/webimager && npx vite dev` and open the URL shown
    2. Upload any photo (drag-and-drop or click to browse)
    3. In the sidebar, expand "Adjustments" section
    4. Drag the Brightness slider -- image should update in real-time
    5. Drag the Contrast slider -- image should update in real-time
    6. Drag the Saturation slider -- image should update in real-time
    7. Click "Greyscale" button -- image should convert to black and white, button should turn blue
    8. With greyscale on, adjust brightness -- brightness change should still be visible on the B&W image
    9. Double-click a slider label (e.g., "Brightness") -- that slider should reset to 100%
    10. Apply some adjustments, then rotate the image -- adjustments should persist through rotation
    11. Download the image -- the downloaded file should include the adjustments (not the original)
    12. Click reset (in Transform section) -- all sliders should return to 100% and greyscale should turn off
  </verify>
  <done>User approves: all sliders provide real-time preview, greyscale toggles correctly, adjustments compose with transforms, download includes adjustments, reset clears all</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` -- all tests pass
2. `npx tsc --noEmit` -- no TypeScript errors
3. `npx vite build` -- builds without errors
4. Visual: sliders control image appearance in real-time
5. Visual: download reflects adjustments
</verification>

<success_criteria>
- Brightness, contrast, saturation sliders visible in Adjustments section of sidebar
- Each slider shows current percentage value and has accessible label
- Double-click label resets individual slider to 100%
- Greyscale button toggles B&W with visual active state
- All adjustments preview in real-time on canvas
- Adjustments compose with each other and with rotate/flip
- Downloaded image includes all applied adjustments
- Reset all clears adjustments back to defaults
</success_criteria>

<output>
After completion, create `.planning/phases/02-adjustments/02-02-SUMMARY.md`
</output>
