---
phase: 07-pan-and-zoom
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/components/ZoomControls.tsx
  - src/components/Editor.tsx
  - src/__tests__/panZoom.test.ts
autonomous: false
requirements:
  - PZ-07

must_haves:
  truths:
    - "Floating zoom controls visible in bottom-right when image is loaded"
    - "Minus button zooms out, plus button zooms in"
    - "Zoom percentage text displays current zoom level"
    - "Clicking zoom percentage toggles between current zoom and fit-to-view"
    - "Controls have glassmorphism styling matching bottom bar"
  artifacts:
    - path: "src/components/ZoomControls.tsx"
      provides: "Floating zoom control widget"
      exports: ["ZoomControls"]
      min_lines: 20
    - path: "src/components/Editor.tsx"
      provides: "ZoomControls rendered in canvas area"
      contains: "ZoomControls"
  key_links:
    - from: "src/components/ZoomControls.tsx"
      to: "src/store/editorStore.ts"
      via: "useEditorStore for zoom state and actions"
      pattern: "useEditorStore.*zoomLevel"
    - from: "src/components/Editor.tsx"
      to: "src/components/ZoomControls.tsx"
      via: "import and render"
      pattern: "ZoomControls"
---

<objective>
Create the floating ZoomControls widget with glassmorphism styling and wire it into the Editor layout. Includes a visual checkpoint for the complete zoom/pan feature.

Purpose: Provides discoverable UI controls for zoom (+/-, percentage display, quick reset) beyond just scroll/pinch gestures.
Output: Floating zoom widget in bottom-right corner, complete pan/zoom feature visually verified.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-pan-and-zoom-move-tool-with-hand-cursor-for-panning-the-image-and-pinch-scroll-zoom-for-magnification/07-01-SUMMARY.md
@.planning/phases/07-pan-and-zoom-move-tool-with-hand-cursor-for-panning-the-image-and-pinch-scroll-zoom-for-magnification/07-RESEARCH.md

<interfaces>
<!-- From store (extended by Plan 01) -->
From src/store/editorStore.ts:
```typescript
// New fields added by Plan 01:
zoomLevel: number;        // 1.0 = fit-to-view
panOffset: { x: number; y: number };
setZoom: (level: number) => void;
setPan: (offset: { x: number; y: number }) => void;
resetView: () => void;
```

From src/utils/zoom.ts (created by Plan 01):
```typescript
export function zoomAtPoint(mouseX: number, mouseY: number, oldZoom: number, newZoom: number, oldPan: { x: number; y: number }): { x: number; y: number };
export function clampZoom(value: number): number;
```

From src/index.css:
```css
.glass {
  background: rgba(255, 255, 255, 0.72);
  backdrop-filter: blur(20px) saturate(180%);
}
```

Accent color: #2A9D8F (via Tailwind arbitrary values)
Icons: lucide-react (Plus, Minus already available)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ZoomControls component and wire into Editor</name>
  <files>src/components/ZoomControls.tsx, src/components/Editor.tsx, src/__tests__/panZoom.test.ts</files>
  <action>
    1. Create `src/components/ZoomControls.tsx`:
       - Import `Plus`, `Minus` from `lucide-react`, `useEditorStore` from store, `clampZoom` from `../utils/zoom`.
       - Subscribe to `zoomLevel`, `setZoom`, `setPan`, `resetView`, `sourceImage` from store.
       - Only render when `sourceImage` is not null (controls always visible when image loaded).
       - Layout: `absolute bottom-14 right-3 z-40` (above bottom bar at 48px). Use `glass` class for glassmorphism. Rounded, flex row, items-center, gap-1, `border border-neutral-200/60 shadow-sm`, `px-2 py-1`.
       - Minus button: `onClick` calls `setZoom(clampZoom(zoomLevel * 0.8))` and `setPan` recalculated for center-of-viewport zoom (use container center as cursor point). Use `p-1 hover:text-[#2A9D8F]` styling. `aria-label="Zoom out"`. Icon size `w-4 h-4`.
       - Zoom percentage text button: displays `Math.round(zoomLevel * 100) + '%'`. `onClick` toggles: if `zoomLevel !== 1`, call `resetView()`; if `zoomLevel === 1`, no-op (or could zoom to previous level, but simpler to just reset). Per user decision: "toggles between current zoom and fit-to-view". Store a `previousZoom` ref — when clicking at non-1.0 zoom, save current zoom/pan and reset; when clicking at 1.0, restore saved zoom/pan. Use `text-xs font-semibold w-12 text-center hover:text-[#2A9D8F]` styling.
       - Plus button: `onClick` calls `setZoom(clampZoom(zoomLevel * 1.25))` and recalculate pan for center zoom. `aria-label="Zoom in"`. Same styling as minus.
       - For +/- button zoom: since there's no cursor position, zoom toward center of the container. The component needs to know container dimensions — pass these as props or compute from a known selector. Simplest: zoom +/- just changes zoom level and adjusts pan to keep center stable using `zoomAtPoint` with container center coordinates. Import `zoomAtPoint` from zoom utils.
       - Accept `containerRect` prop `{ width: number; height: number }` for center-point calculations when using +/- buttons.

    2. Modify `src/components/Editor.tsx`:
       - Import `ZoomControls` from `./ZoomControls`.
       - Render `<ZoomControls />` inside the canvas area div (the `flex-1 overflow-hidden` div), positioned absolutely. The canvas area div needs `relative` added for absolute positioning context.
       - Actually, since ZoomControls positions itself absolutely, it needs a positioned ancestor. The canvas area wrapper in Editor should have `relative` class. Currently it's `<div className="flex flex-col flex-1 overflow-hidden">` — add `relative` to this.

    3. Add tests to `src/__tests__/panZoom.test.ts`:
       - Test ZoomControls renders zoom percentage
       - Test zoom in/out button clicks update store

    4. Add CSS `transition: transform 150ms ease-out` to the Canvas wrapper when zoom changes come from button clicks (not wheel). Use a ref to track whether the last zoom was from a button vs wheel, and conditionally add transition. This makes button-initiated zooms animate smoothly while wheel zooms remain instant.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx vitest run --reporter=verbose && npx tsc --noEmit</automated>
  </verify>
  <done>ZoomControls renders in bottom-right with glassmorphism, +/- buttons zoom, percentage displays and toggles, all tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete pan and zoom feature</name>
  <files>none</files>
  <action>
    Present the complete pan and zoom feature for human verification. All code is already implemented in Plan 01 (zoom/pan engine) and Task 1 above (zoom controls UI). This checkpoint validates the full integrated experience.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>User has approved the visual and interactive behavior of pan/zoom</done>
  <what-built>Complete pan and zoom feature: scroll/pinch zoom (cursor-centered), drag-to-pan when zoomed in, floating zoom controls with glassmorphism, cursor feedback (grab/grabbing), double-click reset, crop mode coexistence</what-built>
  <how-to-verify>
    1. Run `cd /Users/brad/Apps/webimager && npm run dev` and open in browser
    2. Load an image
    3. Verify floating zoom controls appear in bottom-right corner with glassmorphism styling
    4. Scroll wheel up/down — image should zoom in/out centered on cursor position
    5. Zoom in to ~200% — cursor should change to grab (open hand)
    6. Click and drag — image should pan, cursor changes to grabbing (closed hand)
    7. Release drag — cursor returns to grab
    8. Scroll back to fit-to-view (100%) — cursor returns to default, dragging should NOT pan
    9. Click the zoom percentage text — should toggle between current zoom and fit-to-view
    10. Click + button — zooms in. Click - button — zooms out
    11. Double-click canvas — resets to fit-to-view
    12. Enter crop mode (Crop tab) — zoom should reset to fit-to-view
    13. While in crop mode, zoom in, then drag a crop handle — should adjust crop. Drag elsewhere — should pan
    14. Apply or cancel crop — zoom resets to fit-to-view
    15. Load a new image — zoom resets to fit-to-view
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — all tests pass
2. `npx tsc --noEmit` — no type errors
3. Human visual verification of zoom/pan behavior, controls styling, cursor feedback
</verification>

<success_criteria>
- Floating zoom controls render with glassmorphism in bottom-right corner
- +/- buttons zoom in/out
- Percentage text shows current zoom and toggles to fit-to-view on click
- Controls only visible when image is loaded
- Complete zoom/pan feature works as described in CONTEXT.md decisions
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-pan-and-zoom-move-tool-with-hand-cursor-for-panning-the-image-and-pinch-scroll-zoom-for-magnification/07-02-SUMMARY.md`
</output>
