---
phase: 04-background-removal-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/types/editor.ts
  - src/store/editorStore.ts
  - src/workers/backgroundRemoval.worker.ts
  - src/__tests__/editorStore.test.ts
  - src/__tests__/backgroundRemoval.test.ts
autonomous: true
requirements:
  - BGREM-01
  - BGREM-05

must_haves:
  truths:
    - "Store tracks backgroundRemoved boolean and backgroundMask ImageData"
    - "toggleBackground flips backgroundRemoved without losing mask data"
    - "resetAll and setImage clear background removal state"
    - "Worker loads RMBG-1.4 model and returns alpha mask as ImageData"
    - "Worker reports download progress via postMessage"
  artifacts:
    - path: "src/types/editor.ts"
      provides: "BackgroundRemoval types on EditorState"
      contains: "backgroundRemoved"
    - path: "src/store/editorStore.ts"
      provides: "Background removal state and actions"
      contains: "setBackgroundMask"
    - path: "src/workers/backgroundRemoval.worker.ts"
      provides: "Web Worker for model loading and inference"
      contains: "load-model"
    - path: "src/__tests__/editorStore.test.ts"
      provides: "Tests for background removal store actions"
      contains: "background"
    - path: "src/__tests__/backgroundRemoval.test.ts"
      provides: "Tests for worker message protocol"
      contains: "worker"
  key_links:
    - from: "src/store/editorStore.ts"
      to: "src/types/editor.ts"
      via: "imports backgroundRemoved/backgroundMask types"
      pattern: "backgroundRemoved.*boolean"
    - from: "src/workers/backgroundRemoval.worker.ts"
      to: "@huggingface/transformers"
      via: "AutoModel.from_pretrained"
      pattern: "AutoModel"
---

<objective>
Install @huggingface/transformers, extend types and Zustand store with background removal state, create the Web Worker for model loading and inference, and write unit tests.

Purpose: Establishes the data layer (store) and compute layer (worker) that all subsequent plans build on. Without these, there is nothing to hook into or render.
Output: Working store with background state, working worker file, passing tests.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-background-removal-engine/04-RESEARCH.md

@src/types/editor.ts
@src/store/editorStore.ts
@src/__tests__/editorStore.test.ts

<interfaces>
<!-- Existing types the executor needs -->

From src/types/editor.ts:
```typescript
export interface Transforms {
  rotation: 0 | 90 | 180 | 270;
  flipH: boolean;
  flipV: boolean;
}

export interface Adjustments {
  brightness: number;
  contrast: number;
  saturation: number;
  greyscale: boolean;
}

export interface EditorState {
  sourceImage: ImageBitmap | null;
  originalFile: File | null;
  wasDownscaled: boolean;
  transforms: Transforms;
  adjustments: Adjustments;
}
```

From src/store/editorStore.ts:
```typescript
interface EditorStore {
  // ... existing fields ...
  resetAll: () => void;  // Must be extended to clear background state
  setImage: (bitmap: ImageBitmap, file: File, wasDownscaled: boolean) => void;  // Must be extended
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependency, extend types and store with background removal state</name>
  <files>package.json, src/types/editor.ts, src/store/editorStore.ts, src/__tests__/editorStore.test.ts</files>
  <behavior>
    - Test: initial state has backgroundRemoved=false and backgroundMask=null
    - Test: setBackgroundMask(mockImageData) sets backgroundMask and backgroundRemoved=true
    - Test: clearBackgroundMask sets both to null/false
    - Test: toggleBackground flips backgroundRemoved without changing backgroundMask
    - Test: resetAll clears backgroundRemoved and backgroundMask
    - Test: setImage clears backgroundRemoved and backgroundMask
  </behavior>
  <action>
1. Run `npm install @huggingface/transformers` to add the ML library.

2. Extend `src/types/editor.ts`:
   - Add `backgroundRemoved: boolean` and `backgroundMask: ImageData | null` to the `EditorState` interface.

3. Extend `src/store/editorStore.ts`:
   - Add `backgroundRemoved: false` and `backgroundMask: null` to initial state.
   - Add actions to `EditorStore` interface:
     - `setBackgroundMask: (mask: ImageData) => void` -- sets mask and backgroundRemoved=true
     - `clearBackgroundMask: () => void` -- sets mask=null and backgroundRemoved=false
     - `toggleBackground: () => void` -- flips backgroundRemoved (keeps mask intact)
   - Implement the three actions.
   - Extend `resetAll` to also set `backgroundRemoved: false, backgroundMask: null`.
   - Extend `setImage` to also set `backgroundRemoved: false, backgroundMask: null`.

4. Extend `src/__tests__/editorStore.test.ts`:
   - Add `backgroundRemoved: false, backgroundMask: null` to the `beforeEach` setState call.
   - Add a `describe('background removal')` block with the tests from the behavior section above.
   - For mock ImageData, use `{ data: new Uint8ClampedArray(4), width: 1, height: 1 } as unknown as ImageData`.

Write tests first (RED), then implement types and store (GREEN).
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx vitest run src/__tests__/editorStore.test.ts</automated>
  </verify>
  <done>All existing and new editorStore tests pass. Store has backgroundRemoved, backgroundMask, setBackgroundMask, clearBackgroundMask, toggleBackground. resetAll and setImage clear background state.</done>
</task>

<task type="auto">
  <name>Task 2: Create Web Worker for background removal inference</name>
  <files>src/workers/backgroundRemoval.worker.ts, src/__tests__/backgroundRemoval.test.ts</files>
  <action>
1. Create `src/workers/backgroundRemoval.worker.ts` following the complete worker file structure from 04-RESEARCH.md Pattern 2 and Pattern 3. Key details:
   - Import `env, AutoModel, AutoProcessor, RawImage` from `@huggingface/transformers`.
   - Set `env.allowLocalModels = false`.
   - Define `MODEL_ID = 'briaai/RMBG-1.4'`.
   - Use module-level `model` and `processor` variables (typed as `any` per research open question).
   - `self.onmessage` handler switches on `type`:
     - `'load-model'`: calls `loadModel()` which loads model with `config: { model_type: 'custom' }` and the exact processor config from research (image_mean: [0.5,0.5,0.5], image_std: [1,1,1], size: {width:1024,height:1024}, rescale_factor: 0.00392156862745098, etc). Posts `download-progress` messages during model load, then `model-ready`.
     - `'run-inference'`: calls `runInference(e.data.imageData)`. Posts `inference-start`, then processes image through model, converts output tensor to mask ImageData (white RGB + alpha from mask), posts `inference-complete` with `maskData` using transferable `[resultData.data.buffer]`.
   - Wrap both handlers in try/catch, posting `{ type: 'error', message }` on failure.

2. Create `src/__tests__/backgroundRemoval.test.ts` with unit tests for the worker message types:
   - Test file should define the `WorkerInMessage` and `WorkerOutMessage` types (copy from research Pattern 1) and verify they type-check correctly.
   - Test that the worker file can be imported without errors (import the file path exists check).
   - Note: actual inference tests require the real model and are manual-only. These tests verify the message protocol contract types compile and the worker file syntax is valid.
   - Use `describe('background removal worker')` with tests for message type exhaustiveness.
  </action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && npx vitest run src/__tests__/backgroundRemoval.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Worker file exists at src/workers/backgroundRemoval.worker.ts with load-model and run-inference handlers. TypeScript compiles without errors. Worker message type tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (existing + new background removal tests)
- `npx tsc --noEmit` -- no TypeScript errors
- `npm ls @huggingface/transformers` -- package installed
</verification>

<success_criteria>
- @huggingface/transformers installed in package.json
- EditorState type includes backgroundRemoved and backgroundMask fields
- Store has setBackgroundMask, clearBackgroundMask, toggleBackground actions
- resetAll and setImage clear background removal state
- Worker file handles load-model and run-inference messages
- All tests pass, TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/04-background-removal-engine/04-01-SUMMARY.md`
</output>
