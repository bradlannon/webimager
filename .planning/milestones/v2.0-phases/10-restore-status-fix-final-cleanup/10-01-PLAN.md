---
phase: 10-restore-status-fix-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useBackgroundRemoval.ts
  - src/utils/canvas.ts
  - src/__tests__/renderPipeline.test.ts
autonomous: true
requirements: [BGREM-05]
gap_closure: true

must_haves:
  truths:
    - "After clicking Restore Background, UI shows Remove Background button"
    - "User can remove background, restore, then remove again without page reload"
    - "No orphaned drawCheckerboard export in src/utils/canvas.ts"
  artifacts:
    - path: "src/hooks/useBackgroundRemoval.ts"
      provides: "restoreBackground resets status to idle"
      contains: "setStatus('idle')"
    - path: "src/utils/canvas.ts"
      provides: "Clean canvas utilities with no dead exports"
  key_links:
    - from: "src/hooks/useBackgroundRemoval.ts"
      to: "BackgroundControls component"
      via: "status state drives button rendering"
      pattern: "setStatus\\('idle'\\)"
---

<objective>
Fix the restoreBackground status desync bug and remove the orphaned drawCheckerboard export.

Purpose: After restoring a background, the hook status stays 'done' instead of resetting to 'idle', which causes BackgroundControls to render "Restore Background" again instead of "Remove Background". This blocks the remove-restore-remove cycle. Additionally, drawCheckerboard in canvas.ts is dead code (CSS class used instead).
Output: Two files patched, one test file updated, restore/re-remove flow working.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/hooks/useBackgroundRemoval.ts
@src/utils/canvas.ts
@src/__tests__/renderPipeline.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix restoreBackground status desync</name>
  <files>src/hooks/useBackgroundRemoval.ts</files>
  <action>In the `restoreBackground` callback (lines 144-146), add `setStatus('idle')` after the `clearBackgroundMask()` call. The callback currently only calls `useEditorStore.getState().clearBackgroundMask()` but does not reset the hook's local `status` state, leaving it stuck at `'done'`. After the fix, the callback should be:

```typescript
const restoreBackground = useCallback(() => {
  useEditorStore.getState().clearBackgroundMask();
  setStatus('idle');
}, []);
```

This ensures BackgroundControls sees `status === 'idle'` and renders the "Remove Background" button instead of "Restore Background".</action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && grep -A3 'const restoreBackground' src/hooks/useBackgroundRemoval.ts | grep "setStatus('idle')"</automated>
  </verify>
  <done>restoreBackground callback resets status to 'idle', enabling the remove-restore-remove cycle without page reload.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned drawCheckerboard export and its test</name>
  <files>src/utils/canvas.ts, src/__tests__/renderPipeline.test.ts</files>
  <action>1. In `src/utils/canvas.ts`, delete the entire `drawCheckerboard` function (lines 171-185). This function is dead code -- BGREM-04 uses a CSS `.checkerboard-bg` class instead.

2. In `src/__tests__/renderPipeline.test.ts`:
   - Remove `drawCheckerboard` from the import statement on line 2 (keep `limitSize` and `renderToCanvas`)
   - Delete the entire `describe('drawCheckerboard', ...)` test block (starts at line 108)

No other files import drawCheckerboard -- it is only referenced in these two files.</action>
  <verify>
    <automated>cd /Users/brad/Apps/webimager && ! grep -r 'drawCheckerboard' src/ && echo "PASS: no references remain"</automated>
  </verify>
  <done>drawCheckerboard function and its test are removed. No orphaned exports remain in canvas.ts.</done>
</task>

</tasks>

<verification>
1. `grep -A3 'const restoreBackground' src/hooks/useBackgroundRemoval.ts` shows setStatus('idle') call
2. `grep -r 'drawCheckerboard' src/` returns no matches
3. `npx tsc --noEmit` compiles without errors
4. `npx vitest run` passes all tests
</verification>

<success_criteria>
- restoreBackground resets status to 'idle' after clearing mask
- drawCheckerboard function and test fully removed
- TypeScript compiles clean, all tests pass
- Remove-restore-remove cycle works without page reload
</success_criteria>

<output>
After completion, create `.planning/phases/10-restore-status-fix-final-cleanup/10-01-SUMMARY.md`
</output>
